name: Weekly Threat Report

on:
  schedule:
    - cron: "0 0 * * 0"  # Every Sunday at 00:00 UTC
  workflow_dispatch:     # Optional: manual trigger

jobs:
  generate_weekly_report:
    runs-on: ubuntu-latest

    steps:
      # 1. Checkout the repository
      - name: Checkout repository
        uses: actions/checkout@v4

      # 2. Setup Python
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      # 3. Install dependencies
      - name: Install dependencies
        run: pip install -r requirements.txt

      # 4. Call Render weekly report endpoint
      - name: Generate Weekly Report
        run: |
          echo "Calling weekly report endpoint..."
          curl -X GET "${{ secrets.RENDER_APP_URL }}/report/weekly?key=${{ secrets.ADMIN_KEY }}"
     
      # 5. Commit generated reports
      - name: Commit Weekly Reports
        run: |
          git config --global user.name "github-actions"
          git config --global user.email "actions@github.com"
          git add weekly_reports/
          git commit -m "Add weekly threat report" || echo "No changes"
          git push origin main
